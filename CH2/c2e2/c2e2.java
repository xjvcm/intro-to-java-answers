/**
 * (Compute the volume of a triangle) Write a program that reads in the
 * length of sides of an equilateral triangle and computes the area and 
 * volume using the following formulas:
 * area = ((sqrt(3)) / 4) * ((length of sides)^2)
 * volume = area * length
 */
public class c2e2 {

    public static void main(String[] args) {
        
    }
}